# Application
spring.application.name=property-listing-management
server.port=8080

# Microsoft SQL Server database
spring.datasource.url=${DB_URL:jdbc:sqlserver://localhost;instanceName=SQLEXPRESS;databaseName=PropertySalesDB;encrypt=true;trustServerCertificate=true}
spring.datasource.username=${DB_USERNAME:property_app}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA and Hibernate
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true

# Property image uploads
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=6MB
property.image.upload-directory=${PROPERTY_IMAGE_DIR:uploads/property-images}